#include<bits/stdc++.h>
using namespace std;

int main(){

    // Can store Duplicate keys but in sorted order
    // everything same as map, it can store multiple keys
    // only mpp[keys] cannot be used here
    

}
