#include<bits/stdc++.h>
using namespace std;

int main(){

   unordered_set<int> ust; 

    // lower_bound and upper-bound function does not work
    // rest all functions are same as above, it does not store any particular order, 
    // it has a better complexity than set in most cases, except some when collision happens

}
